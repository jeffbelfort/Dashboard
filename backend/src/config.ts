// ── User Configuration ─────────────────────────────────────────────────────
// Change these to your location before running the dashboard

export const config = {
  // Your city name (displayed in the weather widget header)
  city: 'Sheffield',

  // Latitude and longitude — find yours at https://latlong.net
  latitude: 53.3811,
  longitude: -1.4701,

  // WebSocket port (must match the frontend ws://127.0.0.1:PORT)
  wsPort: 3001,
};
